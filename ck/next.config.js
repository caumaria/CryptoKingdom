/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, //this
    swcMinify: true, //this
    compiler: {
        styledComponents: true,
    }
}

module.exports = nextConfig

